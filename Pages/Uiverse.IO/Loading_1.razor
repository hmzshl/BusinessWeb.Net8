<div class="business-loader" role="status" aria-live="polite" aria-label="Chargement">
    <div class="business-loader-orbit">
        <span class="business-loader-ring"></span>
        <span class="business-loader-core">
            <span class="business-loader-mark">B</span>
        </span>
    </div>
    <div class="business-loader-text">Chargement</div>
    <div class="business-loader-dots" aria-hidden="true">
        <span></span>
        <span></span>
        <span></span>
    </div>
</div>

<style>
    .business-loader {
        display: inline-flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        color: #203154;
        font-family: "Ubuntu", system-ui, -apple-system, "Segoe UI", sans-serif;
    }

    .business-loader-orbit {
        position: relative;
        width: 92px;
        height: 92px;
        display: grid;
        place-items: center;
        border-radius: 50%;
        background: radial-gradient(circle at 50% 50%, #ffffff 0%, #f4f7fb 68%, #e7edf6 100%);
        box-shadow: 0 18px 38px rgba(15, 23, 42, 0.16), inset 0 0 0 1px rgba(32, 49, 84, 0.08);
    }

    .business-loader-ring {
        position: absolute;
        inset: 7px;
        border-radius: 50%;
        border: 4px solid #dbe3ef;
        border-top-color: #203154;
        border-right-color: #5a7bb8;
        animation: business-loader-spin 1s linear infinite;
    }

    .business-loader-core {
        width: 46px;
        height: 46px;
        display: grid;
        place-items: center;
        border-radius: 50%;
        background: linear-gradient(135deg, #203154, #315080);
        box-shadow: 0 10px 20px rgba(32, 49, 84, 0.24);
    }

    .business-loader-mark {
        color: #ffffff;
        font-size: 1.25rem;
        font-weight: 700;
        line-height: 1;
    }

    .business-loader-text {
        margin-top: 0.9rem;
        font-size: 0.86rem;
        font-weight: 700;
        color: #203154;
    }

    .business-loader-dots {
        display: inline-flex;
        gap: 0.28rem;
        margin-top: 0.35rem;
    }

    .business-loader-dots span {
        width: 0.36rem;
        height: 0.36rem;
        border-radius: 50%;
        background: #5a7bb8;
        animation: business-loader-pulse 1.1s ease-in-out infinite;
    }

    .business-loader-dots span:nth-child(2) {
        animation-delay: 0.16s;
    }

    .business-loader-dots span:nth-child(3) {
        animation-delay: 0.32s;
    }

    @@keyframes business-loader-spin {
        to {
            transform: rotate(360deg);
        }
    }

    @@keyframes business-loader-pulse {
        0%, 80%, 100% {
            opacity: 0.35;
            transform: translateY(0);
        }

        40% {
            opacity: 1;
            transform: translateY(-3px);
        }
    }
</style>
