@page "/imp-ventes"
@attribute [Authorize(Roles = "Super Admin,Admin,Direction")]



<Documents domaine="0" title="DOCUMENTS VENTES" />

@code {
    bool IsLoaded = false;
    protected override async Task OnInitializedAsync()
    {
        await Task.Delay(1);
        IsLoaded = true;
    }
}