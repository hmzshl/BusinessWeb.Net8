@page "/ot-reglements-clients"
@attribute [Authorize(Roles = "Super Admin,Admin,Direction")]



<Reglements domaine="0" title="REGLEMENTS CLIENTS" />

@code {
    bool IsLoaded = false;
    protected override async Task OnInitializedAsync()
    {
        await Task.Delay(1);
        IsLoaded = true;
    }
}